# Workflow Run Validation Test

This file exists solely to trigger a release-please run that will validate
the workflow_run trigger pattern in publish-on-tag.yml (Phase 3 pilot).

It will be removed in a follow-up commit after validation succeeds.
